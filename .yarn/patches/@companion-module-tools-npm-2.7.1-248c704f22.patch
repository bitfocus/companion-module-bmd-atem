diff --git a/scripts/lib/build-util.js b/scripts/lib/build-util.js
index 167595330f3b13112e257be288f64fb7b0e897be..29f31daaa56c6b6c0abec3a725e6209704382fcf 100644
--- a/scripts/lib/build-util.js
+++ b/scripts/lib/build-util.js
@@ -234,6 +234,12 @@ export async function buildPackage(frameworkPackageName, validateManifest, modul
 		}
 	}
 
+	// Some hacks that need formalizing..
+	const freetypeVendorDir = path.join(packageBaseDir, 'node_modules', '@julusian', 'freetype2', 'vendor')
+	if (fs.existsSync(freetypeVendorDir)) {
+		await fs.rm(freetypeVendorDir, { recursive: true, force: true })
+	}
+
 	// Create tgz of the build
 	let tgzFile = toSanitizedDirname(`${manifestJson.id}-${manifestJson.version}`)
 	if (typeof argv['output'] === 'string') {
